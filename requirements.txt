# 造梦者 (Dream Weaver) 依赖
# 核心依赖
fastapi>=0.141.1
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
requests>=2.31.0

# LLM Provider
openai>=1.30.0

# LangGraph（多 Agent 编排）
langgraph>=0.2.0

# 可选：云端备份（AWS S3 / 阿里云 OSS / MinIO）
boto3>=1.34.0
oss2>=2.18.0

# 可选：番茄小说发布（Playwright 浏览器自动化）
playwright>=1.40.0
